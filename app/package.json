{
  "name": "app",
  "version": "0.0.0",
  "private": true,
  "author": "chiefbiiko",
  "description": "simple faucet frontend",
  "dependencies": {
    "@rebass/forms": "4.0.6",
    "react": "17.0.2",
    "react-dom": "17.0.2",
    "react-modal": "3.14.3",
    "react-redux": "7.2.4",
    "react-router-dom": "5.2.0",
    "react-scripts": "4.0.3",
    "rebass": "4.0.7",
    "redux-thunk": "2.3.0",
    "styled-components": "5.3.0",
    "three-dots": "0.2.1"
  },
  "scripts": {
    "start": "react-scripts --openssl-legacy-provider start",
    "build": "react-scripts --openssl-legacy-provider build",
    "test": "react-scripts test",
    "fmt": "npx prettier --no-semi --arrow-parens avoid --trailing-comma none --print-width 80 --write './src/*.js'"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
